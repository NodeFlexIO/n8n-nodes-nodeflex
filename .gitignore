node_modules
.DS_Store
.tmp
tmp
dist/
library/*.js
library/*.d.ts
library/*.js.map
npm-debug.log*
yarn.lock
.vscode/launch.json

# Build artifacts
*.tgz
tsconfig.tsbuildinfo

# Local secrets for integration tests — never commit real API keys
.env.test.local
.env*.local

# Copilot / editor instructions (internal tooling, not for public consumers)
.github/copilot-instructions.md
.github/*
!.github/workflows/
.vscode/*
node_modules/*

# Development-only tooling carried over from the monorepo checkout — not
# needed to build, test, or publish this package. Live docs/scripts/tests
# for these belong in the development repo, not here.
style.analysis
docs/testing.md
scripts/run-integration-tests.mjs
vitest.integration.config.mts
tests/integration/
